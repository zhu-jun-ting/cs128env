#include <iostream>

#include "dna_strand.hpp"

int main() {
  // Node* first1 = new Node('a');
  // Node* first2 = new Node('b');
  // Node* first3 = new Node('a');
  // Node* first4 = new Node('b');
  // Node* first5 = new Node('a');
  // Node* first6 = new Node('a');
  // Node* first7 = new Node('b');
  // Node* first8 = new Node('a');
  // Node* first9 = new Node('a');
  // first1->next = first2;
  // first2->next = first3;
  // first3->next = first4;
  // first4->next = first5;
  // first5->next = first6;
  // first6->next = first7;
  // first7->next = first8;
  // first8->next = first9;
  // DNAstrand list;
  // list.Addhead(first1);
  // list.Addtail(first6);
  // Node* find = list.Gethead();
  // while (find != nullptr) {
  //   std::cout << find->data << '\n';
  //   find = find->next;
  // }
  // DNAstrand list2;
  // Node* first21 = new Node('t');
  // Node* first22 = new Node('g');
  // Node* first23 = new Node('a');
  // Node* first24 = new Node('t');
  // Node* first25 = new Node('c');
  // first21->next = first22;
  // first22->next = first23;
  // first23->next = first24;
  // first24->next = first25;
  // list2.Addhead(first21);
  // list2.Addtail(first25);
  // list.SpliceIn("ab", list2);
  // find = list.Gethead();
  // while (find != nullptr) {
  //   std::cout << "new" << find->data << '\n';
  //   find = find->next;
  // }
}